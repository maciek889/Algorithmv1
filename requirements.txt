# Pinned exactly for reproducibility. Python >= 3.11.
# Adjustments from spec:
#   - yfinance: spec asked 0.2.40 (does not exist on PyPI). 0.2.39 has a
#     PEP 701 nested-quote f-string that breaks on Python 3.11. Pinned to
#     0.2.38 (last 0.2.x compatible with 3.11; 1.x is a major rewrite).
#   - pandas-ta: spec asked 0.3.14b0 (does not exist on PyPI). Deferred to
#     Stage 1.3 when feature engineering starts and a working version is chosen.

# core
pandas==2.2.3
numpy==1.26.4
pyarrow==15.0.2
pyyaml==6.0.1
python-dotenv==1.0.1

# data sources
yfinance==0.2.38
fredapi==0.5.2
requests==2.32.3

# pre-installed for env consistency with later stages
xgboost==2.1.0
scikit-learn==1.5.0
matplotlib==3.9.0

# testing
pytest==8.2.0
pytest-mock==3.14.0
