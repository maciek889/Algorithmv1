# Pinned exactly for reproducibility. Python >= 3.11.
# Adjustments from spec:
#   - yfinance: spec asked 0.2.40 (does not exist on PyPI). 0.2.x series no
#     longer works with current Yahoo Finance auth (crumb/cookie endpoint
#     changes). Pinned to 1.3.0 (latest stable). Public yf.download API is
#     compatible; loader already handles the MultiIndex column response.
#   - pandas-ta: spec asked 0.3.14b0 (does not exist on PyPI). Deferred to
#     Stage 1.3 when feature engineering starts and a working version is chosen.

# core
pandas==2.2.3
numpy==1.26.4
pyarrow==15.0.2
pyyaml==6.0.1
python-dotenv==1.0.1

# data sources
yfinance==1.3.0
fredapi==0.5.2
requests==2.32.3

# pre-installed for env consistency with later stages
xgboost==2.1.0
scikit-learn==1.5.0
matplotlib==3.9.0

# Stage 1.2 EDA + notebook execution
scipy==1.17.1
nbformat==5.10.4
nbclient==0.10.2
nbconvert==7.16.6
ipykernel==6.29.5

# testing
pytest==8.2.0
pytest-mock==3.14.0
